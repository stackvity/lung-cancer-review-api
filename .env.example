DB_HOST=localhost
DB_PORT=5432
DB_USER=youruser
DB_PASSWORD=yourpassword  #  Sensitive!
DB_NAME=yourdbname
DB_SSL_MODE=disable
DB_MAX_OPEN_CONNS=25 # Example value, tune as needed
DB_MAX_IDLE_CONNS=5  # Example value, tune as needed
DB_CONN_MAX_LIFETIME=30m # Example value, tune as needed
DB_CONN_MAX_IDLE_TIME=5m # Example value, tune as needed
HTTP_SERVER_ADDRESS=:8080
SENTRY_DSN=your_sentry_dsn  #  Sensitive!
SENTRY_ENVIRONMENT=development # or staging, production
LINK_EXPIRATION=24h        # Duration
DATA_RETENTION=90d          # Duration
LOG_LEVEL=info            # debug, info, warn, error, fatal
LOG_FORMAT=text            # text, json
GEMINI_API_KEY=YOUR_GEMINI_API_KEY # Sensitive!
GCLOUD_PROJECT=your-gcloud-project-id # If using Google Cloud.
REPORT_TEMPLATE_PATH=./internal/pdf/templates  #  Relative path to report templates
STORAGE_TYPE=cloud  # cloud, local
CLOUD_STORAGE_BUCKET=your-cloud-storage-bucket # Sensitive!
FILE_ENCRYPTION_KEY=your-very-secret-encryption-key # Sensitive and *REQUIRED* in production!


